import { HowScreen } from "@/components/HowScreen";

export const metadata = {
  title: "How it works — Lighthouse",
  description: "Learn about the four pillars and dynamic methodology of Lighthouse's verification ledger.",
};

export default function Page() {
  return <HowScreen />;
}
